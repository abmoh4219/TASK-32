//! Dashboard page module — the real implementation lives in `app.rs::DashboardHome`.
//! This file exists only to keep the module tree valid; the component is defined
//! next to the router for co-location with the route table.
