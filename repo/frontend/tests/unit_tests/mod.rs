//! Frontend unit-test entry point. Pure-function tests live here so they can run
//! in a native Rust harness without a WASM browser environment.

mod validation_tests;
mod promotion_tests;
mod mask_tests;
mod filter_tests;
